{
  "name": "@ianhomer/gatsby-plugin-things",
  "description": "Gatsby plugin which renders markdown things.",
  "version": "0.0.4",
  "author": "Ian Homer",
  "bugs": {
    "url": "https://github.com/ianhomer/gatsby-plugin-things/issues"
  },
  "dependencies": {
    "@babel/runtime": "^7.13.10",
    "node-fetch": "^2.6.1"
  },
  "keywords": [
    "gatsby",
    "gatsby-source",
    "gatsby-plugin",
    "cat facts"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/ianhomer/gatsby-plugin-things.git"
  },
  "scripts": {
    "build": "babel ./src --out-dir ./",
    "jest": "jest",
    "prepare": "cross-env NODE_ENV=production npm run build",
    "watch": "babel -w src --out-dir .",
    "test": "jest",
    "update": "npx npm-check-updates -u && npm install --package-lock-only --no-audit"
  },
  "devDependencies": {
    "@babel/cli": "^7.13.10",
    "@babel/compat-data": "^7.13.12",
    "@babel/core": "^7.13.13",
    "acorn": ">=8.1.0",
    "babel-preset-gatsby-package": "^1.1.0",
    "cross-env": "^7.0.3",
    "jest": "^26.6.3",
    "lodash": "^4.17.21"
  },
  "license": "MIT",
  "peerDependencies": {
    "gatsby": ">3.1.2"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/"
  }
}
